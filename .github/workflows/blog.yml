name: Rss to Readme

on:
  schedule:
   - cron: '0 * * * *'

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: JasonEtco/rss-to-readme@v1
        with:
          feed-url: https://blognivins.netlify.app/index.xml
          readme-section: feed
